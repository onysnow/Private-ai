## What changed

<!-- One or two sentences: what does this PR do, and why? -->

## Why

<!-- Link the issue this closes, if any: Closes #___ -->

## How this was tested

<!-- Real test runs, not just "should work." Include the command(s) you ran. -->
- [ ] Backend test suite passes (`pytest`)
- [ ] Frontend build/typecheck passes (`npm run build && npm run typecheck`), if frontend touched
- [ ] No real names, companies, or other identifying details introduced into fixtures/tests/UI (see `.github/copilot-instructions.md`)

## Scope check

- [ ] This PR is scoped to one concern and doesn't bundle unrelated changes
- [ ] Stack ownership boundaries respected (see `STACK_OWNERSHIP.md`) — no reinvented FollowTheMoney/OpenAleph functionality
