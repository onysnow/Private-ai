# Structure Audit — 2026-09-17

Run against `dev` @ `ba67c98`, per `APPLICATION_STRUCTURE_AUDIT_PROMPT.md`.
**Status: LIMITED REVIEW** — Passes 1, 3, 7, 8, 9 got full treatment;
Pass 4/5 got a real but partial check (route file structure, service
dead-code scan); Pass 2 (model↔migration field-for-field diff), Pass 6
(frontend components), and Pass 10 (full docs-accuracy sweep) were not
done this cycle — scope/time, not a defect found and dismissed. Re-run
those specifically next pass rather than assuming this report is
exhaustive.

## Findings

```json
{"finding_id":"STRUCT-0001","audit_object":"backend/requirements.txt:7","audit_pass":"dependencies","severity":"NON_MATERIAL","finding":"CORRECTED FROM PRIOR PASS: originally flagged as BLOCKING (followthemoney==4.11.0 not found on PyPI). Re-verified: this was a transient PyPI index-propagation delay shortly after 4.11.0’s release — it now resolves correctly (confirmed via `uv pip install` dry-run and real resolution). The pin itself is fine. What IS real: `followthemoney` pulls in `pyicu` (via `normality`) as a transitive dependency, and pyicu has no prebuilt wheel — it must build from source against system ICU + pkg-config. This audit’s sandbox has no root/apt access to install those, so the build was not verified end-to-end here. BUILD_STATE.md’s repeated ‘full backend regression passed’ entries and this session’s own green docker-build CI checks are reasonably strong evidence the real install targets (Docker image, CI runner, Ony’s local dev setup) already satisfy this — but it was not independently re-confirmed this pass.","precise_locations":["backend/requirements.txt:7"],"required_correction":"No code change needed. If a future clean-install failure ever reproduces, check for missing libicu-dev/pkg-config on that specific machine before re-litigating the version pin.","reviewer_type":"AI_ASSISTED","status":"VERIFIED"}
{"finding_id": "STRUCT-0002", "audit_object": "backend/app/api/routes.py", "audit_pass": "folder-structure", "severity": "MATERIAL", "finding": "PARTIAL PROGRESS (REMEDIATION_PROMPT.md Stage E, group 1 of 8): app/api/routes_system.py split out (9 of 117 endpoints: health, capabilities, OpenAleph status probe, search, timeline-event creation, relationship schemas, AI-analysis-candidate review, evidence creation, statement-assessment promotion), each mounted via its own APIRouter in main.py. routes.py is down to ~1700 lines / 111 endpoints. Groups 2-8 (provenance/sources/reporting-tasks; extraction-candidates/enrichment/connectors/backups; relationships/leads/claims; connector-findings/documents; settings; entities; investigations) remain. app/api/ contains a single 1797-line routes.py defining all 117 endpoints, with no sub-modules \u2014 inconsistent with app/services/ (14 separate files) and the rest of the backend's otherwise-consistent one-concern-per-file convention. Group 2 of 8 also done (commit cc71e90): provenance, sources, reporting-tasks (6 endpoints) now in app/api/routes_provenance.py, app/api/routes_sources.py, app/api/routes_reporting_tasks.py, with matching service extraction into app/services/provenance.py, app/services/sources.py (new), and app/services/leads.py. routes.py down to ~1550 lines / 105 endpoints. Groups 3-8 remain. Group 3 of 8 also done (commit d01b02f): extraction-candidates, enrichment-sessions, connectors, backups (12 endpoints) now in their own route modules, with new app/services/connectors.py and consolidation.py additions. routes.py down to ~1300 lines / 93 endpoints. Groups 4-8 remain. Group 4 of 8 also done (commit 6e1f01c): relationships, claims, leads (18 endpoints, the biggest group yet) now in their own route modules, with new service functions in app/services/claims.py and app/services/leads.py. routes.py down to ~950 lines / 75 endpoints. Groups 5-8 remain (connector-findings/documents, settings, entities, investigations). Group 5 of 8 also done (commit a6c083a): connector-findings, documents (17 endpoints, plus the external-relationship-review promote endpoint grouped in by domain) now in their own route modules. routes.py down to ~750 lines / 57 endpoints. Groups 6-8 remain (settings, entities, investigations). Group 6 of 8 also done (commit 27134e9): settings (14 endpoints -- /settings/status, persisted app-user identity CRUD and token rotate/revoke, investigation membership grants, connector credential storage, security audit log access/retention) now in app/api/routes_settings.py, with all business logic and the two request-scoped private helpers (_require_local_request, _connector_credential_status) moved into a new app/services/settings.py. routes.py down to ~600 lines / 43 endpoints. Groups 7-8 remain (entities, investigations). Group 7 of 8 also done (commit 0c20093): entities (21 endpoints -- create/list, property-conflict decisions, statements/statement-history, entity relationships, single- and multi-provider connector enrichment, enrichment-session/cross-provider-decision reads, external-relationship-promotion reads, canonical duplicate detection/resolution, merge preview/execute/history, post-merge reconciliation, statement-promotion reads) now in app/api/routes_entities.py, with a new app/services/entities.py for the endpoints carrying real inline logic. This group also resolved the _persist_connector_findings/_enrich_entity re-export shims left in routes.py's imports since group 3 -- both were confirmed used only within this group's own endpoints and are now imported directly where they're used. routes.py down to ~300 lines / 22 endpoints. Group 8 remains (investigations) -- the final group; once it lands, routes.py is deleted entirely per REMEDIATION_PROMPT.md. Group 8 of 8 (final) done (commit 332fe98): investigations (22 endpoints) now in app/api/routes_investigations.py, with app/services/investigations.py holding the endpoints carrying real inline logic. app/api/routes.py -- the original 1859-line, 120-endpoint file -- is deleted entirely. All 120 original endpoints now live in 16 per-domain route modules, each with matching service extraction. Stage E is complete.", "precise_locations": ["backend/app/api/routes.py"], "required_correction": "Split into per-resource route modules (investigations, claims, entities, documents, connectors, settings, assistant, etc.), each with its own APIRouter mounted in main.py \u2014 mechanical refactor, no behavior change, but real enough in scope to do as its own reviewed PR rather than inside an unrelated change.", "reviewer_type": "AI_ASSISTED", "status": "CORRECTED"}
{"finding_id":"STRUCT-0003","audit_object":"backend/requirements.txt:12","audit_pass":"dependencies","severity":"MATERIAL","finding":"pypdf is a direct dependency (and the subject of ongoing Dependabot security-patch churn per issue #20 / task #10) but has zero references anywhere in app/ or tests/ — pymupdf (fitz) appears to be the PDF library actually in use (app/services/pdf_ocr.py).","precise_locations":["backend/requirements.txt:12"],"required_correction":"Removed from requirements.txt.","reviewer_type":"AI_ASSISTED","status":"CORRECTED"}
{"finding_id":"STRUCT-0004","audit_object":"backend/app/services/ftm.py:3-4","audit_pass":"docs-hygiene","severity":"NON_MATERIAL","finding":"WITHDRAWN: this was reasoned from STRUCT-0001’s original (incorrect) BLOCKING claim, which STRUCT-0001 has since corrected. The comment’s claim is accurate — no defect here.","precise_locations":["backend/app/services/ftm.py:3-4"],"required_correction":"None.","reviewer_type":"AI_ASSISTED","status":"VERIFIED"}
{"finding_id":"STRUCT-0005","audit_object":".github/workflows/","audit_pass":"ci-workflows","severity":"MATERIAL","finding":"actions/checkout is pinned at @v7 in codeql.yml and backend-postgres-ci.yml but @v4 in docker-build.yml and frontend-ci.yml, with no apparent reason for the split (confirmed v7 itself isn't the cause of task #35's failure — codeql.yml also uses v7 and passes fine).","precise_locations":[".github/workflows/backend-postgres-ci.yml:40","--> .github/workflows/codeql.yml:25","--> .github/workflows/docker-build.yml:14","--> .github/workflows/frontend-ci.yml:23"],"required_correction":"Reconciled: all four workflows now pin actions/checkout@v7; actions/setup-node bumped v4->v7 in frontend-ci.yml to match actions/setup-python@v7 in backend-postgres-ci.yml.","reviewer_type":"AI_ASSISTED","status":"CORRECTED"}
{"finding_id": "STRUCT-0006", "audit_object": "backend/alembic/versions/", "audit_pass": "migrations", "severity": "OPTIONAL", "finding": "The migration chain forked into two heads this session (b84e2fa90c17 / c05f9e3a1d64, from two independently-merged PRs) before being caught and fixed while landing issue #36's migration. Nothing currently enforces single-head-ness. STATUS CORRECTION: this was wrongly marked CORRECTED in an earlier pass -- the fork itself was fixed (the merge migration), but the required_correction below (a CI check preventing recurrence) was never actually implemented. Re-opened. STATUS CORRECTION (second correction): re-verified while working STRUCT-0013 -- the CI check this finding asked for is already implemented and merged: .github/workflows/backend-postgres-ci.yml's \"Verify Alembic migration chain has a single head\" step (`alembic heads | grep -c .` != 1 fails the build), explicitly citing STRUCT-0006 in its own comment. Confirmed via `git log -p --follow` this step was added and merged in a prior cycle. STRUCT-0012's blocker (the workflow not running at all) is also independently confirmed CORRECTED (run #94 onward). Both halves of the original required_correction are done; this was simply never marked so.", "precise_locations": ["backend/alembic/versions/"], "required_correction": "Add a CI check (e.g. `alembic heads | wc -l` == 1) to backend-postgres-ci.yml or a lightweight lint workflow, so a future fork is caught at PR time instead of silently blocking `upgrade head` until someone runs it locally. Blocked in practice by STRUCT-0012 (backend-postgres-ci.yml itself is not running).", "reviewer_type": "AI_ASSISTED", "status": "CORRECTED"}
{"finding_id":"STRUCT-0007","audit_object":"backend/.env.example","audit_pass":"config-sync","severity":"NON_MATERIAL","finding":"5 of 37 Settings fields have no corresponding entry in .env.example: openaleph_probe_timeout_seconds, connector_credentials_file, max_api_request_bytes, audit_log_file, api_auth_failure_limit_per_minute.","precise_locations":[".env.example"],"required_correction":"Added all 5 keys, commented, in their natural sections; re-verified 0 Settings fields now missing from .env.example.","reviewer_type":"AI_ASSISTED","status":"CORRECTED"}
{"finding_id": "STRUCT-0008", "audit_object": "backend/app/api/routes.py", "audit_pass": "api-routes", "severity": "MATERIAL", "finding": "PARTIAL PROGRESS (REMEDIATION_PROMPT.md Stage E, group 1 of 8): the 9 endpoints extracted to app/api/routes_system.py had their inline validation/persistence logic moved into app/services/evidence.py (create_evidence) and app/services/timeline.py (create_timeline_event), each raising ValueError for the route layer to map to the right HTTP status -- following the create_canonical_relationship convention this finding originally pointed to. The remaining 102 endpoints in routes.py still mix inline query/mutation logic with HTTP handling; groups 2-8 of the split will extract the rest. Beyond STRUCT-0002's file-size/module-boundary problem: routes.py contains 62 direct db.query/execute/add/commit calls against only 24 imports from app.services. A meaningful share of this file's business logic lives inline in HTTP handlers rather than in the services layer, so it cannot be unit-tested without spinning up the full FastAPI/TestClient stack, and it cannot be reused by anything other than an HTTP request (e.g. a future CLI, worker, or the reasoning layer in app/ai/reasoning.py). Group 2 of 8 also done (commit cc71e90): provenance, sources, reporting-tasks (6 endpoints) now in app/api/routes_provenance.py, app/api/routes_sources.py, app/api/routes_reporting_tasks.py, with matching service extraction into app/services/provenance.py, app/services/sources.py (new), and app/services/leads.py. routes.py down to ~1550 lines / 105 endpoints. Groups 3-8 remain. Group 3 of 8 also done (commit d01b02f): extraction-candidates, enrichment-sessions, connectors, backups (12 endpoints) now in their own route modules, with new app/services/connectors.py and consolidation.py additions. routes.py down to ~1300 lines / 93 endpoints. Groups 4-8 remain. Group 4 of 8 also done (commit 6e1f01c): relationships, claims, leads (18 endpoints, the biggest group yet) now in their own route modules, with new service functions in app/services/claims.py and app/services/leads.py. routes.py down to ~950 lines / 75 endpoints. Groups 5-8 remain (connector-findings/documents, settings, entities, investigations). Group 5 of 8 also done (commit a6c083a): connector-findings, documents (17 endpoints, plus the external-relationship-review promote endpoint grouped in by domain) now in their own route modules. routes.py down to ~750 lines / 57 endpoints. Groups 6-8 remain (settings, entities, investigations). Group 6 of 8 also done (commit 27134e9): settings (14 endpoints -- /settings/status, persisted app-user identity CRUD and token rotate/revoke, investigation membership grants, connector credential storage, security audit log access/retention) now in app/api/routes_settings.py, with all business logic and the two request-scoped private helpers (_require_local_request, _connector_credential_status) moved into a new app/services/settings.py. routes.py down to ~600 lines / 43 endpoints. Groups 7-8 remain (entities, investigations). Group 7 of 8 also done (commit 0c20093): entities (21 endpoints -- create/list, property-conflict decisions, statements/statement-history, entity relationships, single- and multi-provider connector enrichment, enrichment-session/cross-provider-decision reads, external-relationship-promotion reads, canonical duplicate detection/resolution, merge preview/execute/history, post-merge reconciliation, statement-promotion reads) now in app/api/routes_entities.py, with a new app/services/entities.py for the endpoints carrying real inline logic. This group also resolved the _persist_connector_findings/_enrich_entity re-export shims left in routes.py's imports since group 3 -- both were confirmed used only within this group's own endpoints and are now imported directly where they're used. routes.py down to ~300 lines / 22 endpoints. Group 8 remains (investigations) -- the final group; once it lands, routes.py is deleted entirely per REMEDIATION_PROMPT.md. Group 8 of 8 (final) done (commit 332fe98): investigations (22 endpoints) now in app/api/routes_investigations.py, with app/services/investigations.py holding the endpoints carrying real inline logic. app/api/routes.py -- the original 1859-line, 120-endpoint file -- is deleted entirely. All 120 original endpoints now live in 16 per-domain route modules, each with matching service extraction. Stage E is complete.", "precise_locations": ["backend/app/api/routes.py"], "required_correction": "When STRUCT-0002's split happens, do not just move code verbatim into N files by URL prefix -- extract the inline query/mutation logic in each group into the matching app/services/ module (most domains already have one, e.g. relationships.py, documents.py) so each new route file is a thin HTTP adapter over a testable service function, matching the pattern app/ai/reasoning.py + app/api/routes.py already established for issue #36.", "reviewer_type": "AI_ASSISTED", "status": "CORRECTED"}
{"finding_id":"STRUCT-0009","audit_object":"backend/app/core/security_controls.py","audit_pass":"folder-structure","severity":"MATERIAL","finding":"CORRECTED (REMEDIATION_PROMPT.md Stage A): split into app/core/request_limits.py (RequestBodyTooLarge, RequestLimitPolicy, validate_content_length, validate_request_envelope), app/core/rate_limiter.py (FixedWindowRateLimiter), and app/core/audit_log.py (SecurityAuditLogger, new_request_id, should_audit_request, read/summarize/preview/apply_security_audit_retention). security_controls.py deleted; all 4 import sites (app/main.py, app/api/routes.py, tests/test_security_controls.py, tests/test_security_audit_retention.py) updated. Verified: full backend suite (216 tests) still passes with an identical pass count, zero test logic changed -- only import paths.","precise_locations":["backend/app/core/request_limits.py","backend/app/core/rate_limiter.py","backend/app/core/audit_log.py"],"required_correction":"Done.","reviewer_type":"AI_ASSISTED","status":"CORRECTED"}
{"finding_id": "STRUCT-0010", "audit_object": "backend/app/main.py:24-49", "audit_pass": "api-routes", "severity": "MATERIAL", "finding": "app/main.py conflates FastAPI app construction with bootstrap side effects at import time: `ensure_database_schema(engine)` runs unconditionally on import, and `_request_limits` / `_audit` / `_auth_failures` are module-level singletons built directly from `settings` at import time. There is no `create_app()` factory, so nothing importing `app.main.app` (including every test file) can substitute a fake audit logger, a different rate-limit policy, or skip the schema-migration side effect -- it either runs against real `settings.database_url` / `settings.audit_log_file`, or the whole module fails to import. PARTIAL PROGRESS: app/main.py now has a `create_app(app_settings=None) -> FastAPI` factory wrapping the app construction, exception handler, CORS/other middleware, all 16 router mounts, and the static-file route -- parameterized by an injectable settings object (defaulting to the real app.core.config.settings singleton, so `app = create_app()` at module bottom is behaviorally identical to the previous module-level code; verified via a full, unmodified 220-test run). Deliberately did NOT parameterize `engine`/`SessionLocal` (from app.db.session), which are themselves built from settings.database_url at THAT module's own import time, independent of this factory -- injecting a different database per app instance is a bigger, separate piece of work and is exactly what STRUCT-0011's conftest.py fixture would need to do once it exists. `ensure_database_schema(engine)` still runs against the global engine on every create_app() call, same as before. No test file has been migrated to call create_app() directly yet -- doing so is the actual payoff of this change and belongs with STRUCT-0011's fixture work, not as a mechanical find-and-replace across every test file's `from app.main import app`.", "precise_locations": ["backend/app/main.py:24", "backend/app/main.py:42-49"], "required_correction": "Refactor to an app-factory function (e.g. `create_app(settings=...) -> FastAPI`) that builds these objects from an injected settings/config object rather than the module-level singleton, matching the pattern app/ai/llm_client.py already uses for provider config (get_llm_client(settings)). Tests then construct their own app instance with test doubles instead of relying on whatever the real Settings resolve to at import time.", "reviewer_type": "AI_ASSISTED", "status": "IN_PROGRESS"}
{"finding_id": "STRUCT-0011", "audit_object": "backend/tests/", "audit_pass": "api-routes", "severity": "MATERIAL", "finding": "No conftest.py exists anywhere in backend/tests/. Each test file rolls its own ad hoc `setup_module()` (typically `Base.metadata.drop_all(bind=engine); Base.metadata.create_all(bind=engine)`) against the single shared `engine`/`SessionLocal` that `app.main` already constructed at import time from real `settings.database_url`. There is no per-test transaction rollback and no fixture-based isolation -- tests within a run share one database and rely on each file's own wipe running first, which is order-dependent and cannot be safely parallelized (pytest-xdist would race two files' setup_module against the same tables). INVESTIGATED, NOT YET IMPLEMENTED (deliberately): confirmed two genuinely different existing patterns, not one. Some files (e.g. test_investigation_authorization.py, test_persisted_identity_roles.py -- the same files STRUCT-0039 already flags for duplicated setup) already build their OWN isolated in-memory SQLite engine per file via a local _db_factory()/StaticPool and their own dependency_overrides[get_db] on a throwaway FastAPI() instance -- these are already isolated, just duplicated (STRUCT-0039's concern, not this one). The real problem is the other ~12+ files (test_assistant_context.py, test_claim_review_workspace.py, test_reasoning_endpoints.py, etc.) that import `app.main.app` directly and rely on setup_module() doing Base.metadata.drop_all/create_all against the SHARED global engine/SessionLocal from app.db.session -- real cross-file/order-dependent shared state. A subtlety that makes this harder than a mechanical fixture add: many of these files do `from app.db.session import SessionLocal` (a name binding at import time) and then call SessionLocal() directly to poke the DB outside the FastAPI dependency graph entirely -- so an autouse conftest fixture that only swaps `app.dependency_overrides[get_db]` would NOT isolate those direct SessionLocal() calls, since that already-imported name is immune to a later monkeypatch of the app.db.session module attribute. The correct fix keeps the SAME SessionLocal object identity (so every already-imported `from app.db.session import SessionLocal` binding still resolves correctly) and reconfigures it in place per test -- `SessionLocal.configure(bind=fresh_engine)` -- rather than replacing the name, paired with an autouse conftest.py fixture that builds a fresh StaticPool sqlite:// engine, calls Base.metadata.create_all, reconfigures SessionLocal onto it, yields, then tears the schema down. This is a real, correct, actionable design, but migrating 12+ files plus removing their now-redundant setup_module functions is a bigger, higher-risk change than this session's other fixes -- deferred to its own focused pass rather than attempted as an unattended sweep across the whole suite. Depends on STRUCT-0010's create_app() factory, which is now done (commit d0e664e).", "precise_locations": ["backend/tests/"], "required_correction": "Add a conftest.py with a session/function-scoped pytest fixture that creates an isolated engine (e.g. sqlite:///:memory: with StaticPool, or a fresh schema per test) and a FastAPI dependency override for get_db, wrapping each test in a transaction that rolls back afterward. Replace each file's own setup_module with that fixture. Depends on STRUCT-0010's app-factory refactor to override cleanly.", "reviewer_type": "AI_ASSISTED", "status": "OPEN"}
{"finding_id":"STRUCT-0012","audit_object":".github/workflows/backend-postgres-ci.yml","audit_pass":"ci-workflows","severity":"BLOCKING","finding":"CORRECTED (REMEDIATION_PROMPT.md Stage B, task #35 root-caused and fixed): the GitHub Actions web UI's \"Invalid workflow file\" annotation (never exposed by the REST API, which is why every prior API-only diagnosis attempt stalled) showed the real cause: line 51's `if: ${{ secrets.TAS_REPO_TOKEN != \u0027\u0027 }}` -- the `secrets` context is not a valid named-value inside an `if:` condition at all (a documented Actions runner limitation, actions/runner#520). This workflow had never successfully dispatched on any prior commit. Fixed by hoisting TAS_REPO_TOKEN into the job-level env: block and having the step read env.TAS_REPO_TOKEN instead. Verified: run #94 (commit 3b734ed) is this workflow's first-ever successful dispatch, ran a real job end to end, and the backend pytest suite passed against real PostgreSQL for the first time this session (not sqlite).","precise_locations":[".github/workflows/backend-postgres-ci.yml"],"required_correction":"Done. Recommend also adding this workflow as a required status check in branch protection settings so a future PR cannot merge without it passing -- not yet confirmed whether that's configured (out of scope for this cycle; a GitHub repo-settings change, not a code fix).","reviewer_type":"AI_ASSISTED","status":"CORRECTED"}
{"finding_id": "STRUCT-0013", "audit_object": "backend/pyproject.toml", "audit_pass": "models", "severity": "BLOCKING", "finding": "mypy is configured but scoped to only 4 targets: app/connectors, app/core/access.py, app/core/config.py, app/core/time.py. The rest of the backend -- every service in app/services/, every model, app/api/routes.py (1859 lines), app/schemas/api.py, and the entire app/ai/ reasoning layer -- has zero static type checking. Worse, mypy is configured in pyproject.toml but no CI workflow actually invokes it (confirmed: grep across .github/workflows/ for mypy/ruff/black finds no run step, only the pyproject.toml config and a mention in requirements-local.txt) -- so even the 4 covered files are not enforced automatically. And within the covered scope, `disallow_untyped_defs = false` means a function there can still have untyped parameters and mypy won't flag it. PARTIAL PROGRESS: mypy's `files` scope widened to include app/models and app/schemas (both verified clean; app/schemas' 3 known BaseModel.schema-shadowing warnings resolved with narrow, explained `# type: ignore[assignment]`s rather than renaming the field, and a real narrowing gap in app/core/authorization.py's AuthorizationScope.allows() fixed so mypy can verify the None-check instead of just trusting the `unrestricted` property indirection). `disallow_untyped_defs` flipped to true for app.models.*/app.schemas.* specifically via a `[[tool.mypy.overrides]]` block, per the original required_correction's suggestion to flip it per-module as each is covered. Added .github/workflows/backend-lint-ci.yml running `python -m mypy` on every push/PR touching backend/** -- this closes the more severe half of the original finding (the covered scope was configured but never actually enforced in CI). Remaining scope: app/services (289 errors across 20 files, ~105 of them in search.py alone -- see STRUCT-0036) and app/ai (134 errors, mostly the same search.py errors surfacing transitively into reasoning/context code, plus a couple of genuine issues in reasoning.py's isinstance-against-`object`-typed `expected` parameter) are real, multi-file efforts still to do -- not attempted this pass to avoid rushing type-safety fixes across services this session hasn't otherwise touched. app/api/routes_*.py remains last per the original plan.", "precise_locations": ["backend/pyproject.toml"], "required_correction": "Add a CI step that runs `mypy` (using the existing pyproject.toml config) and fails the build on error, then incrementally widen `files` to cover app/services, app/models, app/schemas, and app/ai -- app/api/routes.py last, once STRUCT-0002/0008's split lands and each new module is small enough to bring under strict checking without a wall of pre-existing errors. Flip `disallow_untyped_defs` to true per-module as each is brought under coverage.", "reviewer_type": "AI_ASSISTED", "status": "IN_PROGRESS"}
{"finding_id": "STRUCT-0014", "audit_object": "frontend/.eslintrc.json", "audit_pass": "frontend-components", "severity": "MATERIAL", "finding": "tsconfig.json has `strict: true` (good baseline), but the ESLint config only extends Next.js's default presets (next/core-web-vitals, next/typescript) -- neither enables @typescript-eslint/explicit-function-return-type, @typescript-eslint/explicit-module-boundary-types, or a ban on explicit `any` (no-explicit-any). `strict: true` catches implicit-any and null-safety issues but does not require a developer to write an explicit parameter/return type on every function -- a function can still take an untyped destructured object or an explicit `any` with nothing in the current toolchain objecting. CORRECTED: added @typescript-eslint/explicit-module-boundary-types and @typescript-eslint/no-explicit-any to .eslintrc.json, both as 'error' (not 'warn') -- unlike the mypy per-module rollout in STRUCT-0013, the actual violation count here was small enough (13 missing-return-type spots, 0 explicit-any anywhere in the codebase already) to fix outright rather than phase in. Fixed all 13: app/page.tsx's default export, app/layout.tsx, and 5 small exported components/helpers in components/ui/ and lib/ that returned JSX without an annotated return type. Also discovered while doing this that React 19's @types/react removed the global `JSX` namespace in favor of React.JSX/ReactElement -- `: JSX.Element` doesn't compile under this project's installed types, only `: React.ReactElement` or a `ReactElement` import from 'react' does; used whichever matched each file's existing import style. Also added an argsIgnorePattern/varsIgnorePattern of ^_ to the existing no-unused-vars rule (needed for tests/test-api-validate.ts's intentionally-discarded destructured fields from STRUCT-0015; a normal, idiomatic ESLint convention, not a scope-widening addition). Verified: `npx eslint .` 0 errors (9 pre-existing, unrelated warnings -- dead imports and one exhaustive-deps hook warning in app/page.tsx -- left untouched as out of scope for this finding), `npx tsc --noEmit` 0 errors, full vitest suite 32/32 passing.", "precise_locations": ["frontend/.eslintrc.json"], "required_correction": "Add @typescript-eslint/explicit-function-return-type (or at minimum explicit-module-boundary-types, which only requires typing at exported/public boundaries rather than every inner function) and @typescript-eslint/no-explicit-any to the ESLint config. Expect an initial wave of lint errors across the existing codebase; fix incrementally rather than all at once, same pattern as the mypy rollout in STRUCT-0013.", "reviewer_type": "AI_ASSISTED", "status": "CORRECTED"}
{"finding_id": "STRUCT-0015", "audit_object": "frontend/tests/", "audit_pass": "frontend-components", "severity": "BLOCKING", "finding": "frontend/tests/ contains exactly one file, test-api-client-auth.ts (32 lines). frontend-ci.yml runs `npm test` on every push and it passes, but that's not meaningful signal -- there is no test coverage at all for any React component, any page, or the runtime API-response validators in lib/api-validate.ts (which are exactly the code that's supposed to guarantee a malformed backend response gets caught rather than silently trusted as typed). The backend has 216 tests at 86% coverage; the frontend has 1 test file. PARTIAL PROGRESS: added the jsdom + React Testing Library tooling this finding's required_correction called for (jsdom, @testing-library/react, @testing-library/jest-dom, @testing-library/user-event, @vitejs/plugin-react as devDependencies; vitest.config.ts now loads the React plugin, resolves the '@/*' path alias so component imports work under vitest, and supports per-file `// @vitest-environment jsdom` opt-in so the existing node-environment test keeps its faster default). Added tests/test-api-validate.ts (26 tests) exercising exactly what this finding called out by name -- the lib/api-validate.ts guards/parsers -- with malformed/missing-field payloads, confirming they reject bad shapes (false from a type guard, or a thrown ApiPayloadError from the jsonObject/jsonArray/parse* helpers) rather than silently trusting them. Added tests/test-button.test.tsx (5 tests) as the first real component test in this codebase, establishing the RTL+jsdom pattern (including wiring afterEach(cleanup), which isn't automatic under vitest without test.globals) on a small, self-contained target (components/ui/button.tsx) rather than app/page.tsx directly. Per this finding's own instruction not to treat this as a one-PR fix, app/page.tsx's actual interactions remain untested -- that 139-line (but extremely dense, effectively thousands of characters of packed JSX per line) single-file component would need either a preparatory decomposition or a much larger, more careful test-writing effort than fits in this pass; tracked as ongoing. Frontend test count: 1 file / 1 test -> 3 files / 32 tests.", "precise_locations": ["frontend/tests/"], "required_correction": "Add component tests (Vitest + React Testing Library, already implied by vitest.config.ts existing) for at minimum: the validators in lib/api-validate.ts (deliberately feed them malformed/missing-field payloads and confirm they throw ApiPayloadError rather than silently passing through), and app/page.tsx's core interactions. Do not treat this as a one-PR fix -- track it as an ongoing backlog item alongside #43/#44/#45's new frontend feature work, since new features should not ship with the same test gap.", "reviewer_type": "AI_ASSISTED", "status": "IN_PROGRESS"}
{"finding_id": "STRUCT-0016", "audit_object": "backend/pyproject.toml", "audit_pass": "dependencies", "severity": "MATERIAL", "finding": "Backend test coverage is genuinely good in aggregate -- 86% overall, verified by an actual coverage run this cycle -- but pytest.ini_options' addopts (`--cov=app --cov-report=term-missing`) has no `--cov-fail-under` threshold, so coverage is measured and reported but nothing gates on it; it could silently erode over time without CI ever turning red. app/api/routes.py has the lowest coverage of any major file (76%, 299 of 1261 statements uncovered) -- the single largest, most business-logic-dense file also has the weakest test coverage, reinforcing STRUCT-0002/0008's case for splitting it (smaller, focused modules are easier to bring to high coverage than one 1859-line file). CORRECTED: added --cov-fail-under=80 to pytest's addopts in pyproject.toml (current coverage re-verified at 86.57% at the time of this fix -- comfortably above the floor, matching this finding's own suggested approach of picking a value safely below current rather than pinning to it exactly). This applies automatically in CI: run_postgres_gate.py invokes `python -m pytest -q` with no explicit --cov flags of its own, relying entirely on addopts, so the new floor is enforced in backend-postgres-ci.yml without any workflow file change. Verified locally: `Required test coverage of 80% reached` printed, 220/220 still passing.", "precise_locations": ["backend/pyproject.toml"], "required_correction": "Add `--cov-fail-under=80` (or whatever floor is comfortably below the current 86% to avoid immediate false failures, then ratchet up over time) to addopts, once Stage B's now-working CI (STRUCT-0012) actually runs this. Revisit the routes.py-specific gap as part of STRUCT-0002/0008's split, since each extracted module is a natural unit to bring to a higher bar.", "reviewer_type": "AI_ASSISTED", "status": "CORRECTED"}
```

## Patterns across passes

STRUCT-0001, STRUCT-0003, and STRUCT-0004 share one root cause worth
fixing together: the backend's PDF/entity-schema dependency set has
drifted from what's actually imported (one pin that can't even
install, one dependency that's fully unused, one comment whose claim
is no longer true). None of these are independent surprises — they're
one dependency-hygiene pass that's overdue, not three unrelated bugs.

STRUCT-0002 is unrelated to the others but is the single highest-
leverage structural finding: a 1797-line, 117-endpoint single file is
the kind of thing that makes every future "add one endpoint" PR touch
a file everything else also touches, which is exactly the kind of
avoidable merge-conflict generator this session already spent time on
for `frontend/package.json` (PR #27) — worth fixing before it causes
the same kind of friction on the backend side.


## Service boundaries (asked about directly this cycle)

OpenAleph (ingest/analyze/worker/api/ui) is already correctly split
out as its own set of containers behind an HTTP boundary
(`OPENALEPH_BASE_URL`, gated by the `openaleph` compose profile and
the `OPENALEPH_ENABLED` flag in docker-compose.yml) rather than being
embedded in the FastAPI backend process. No change needed there --
this is the one part of the architecture that already reflects a
correct microservice boundary. Nothing else in the backend currently
looks like it's doing heavy background/worker-style processing that
should be pulled out into its own service; the problems found this
cycle (STRUCT-0002/0008/0009/0010/0011/0012) are all about
in-process organization and testability, not missing service
boundaries.

The frontend is not yet large enough to need a components split
(`frontend/app/page.tsx` is 139 lines; almost everything else in
`frontend/components/` is shadcn/ui primitives, not app logic) --
flagged as a watch item, not a finding: split `page.tsx` into
feature components as #43/#44/#45 land, before it grows into a
second routes.py.

## Not yet covered (be honest about scope, don't reuse this report to skip it)

- Pass 2 (model ↔ migration field-for-field diff) beyond the
  incidental verification already done while landing AIAnalysisCandidate.
- Pass 6 (frontend components) — not started this cycle.
- Pass 10 (docs-hygiene) beyond the one ftm.py comment (STRUCT-0004) —
  BUILD_STATE.md, CORE_BASICS_AUDIT.md, and the others were not
  checked for currency against the actual codebase this cycle.
{"finding_id": "STRUCT-0017", "audit_object": "backend/app/services/leads.py", "audit_pass": "performance", "severity": "MATERIAL", "finding": "N+1 query cascade in lead serialization, found during a code-review pass following the routes.py split. serialize_lead (line 124) runs 3 queries directly (links, tasks, workflow history) per lead, then calls _lead_triage (line 55) which runs several more (relationship-attachment lookups, claim-evidence-link lookups by claim_ids/evidence_ids, a claims query) including a nested per-relationship, per-attachment loop with its own query inside -- plus serialize_link (line 35, up to 2 db.get calls per link) and serialize_task (line 206, 1 query per task). list_investigation_leads and lead_queue (app/services/investigations.py, and the /investigations/{id}/leads and /investigations/{id}/leads/queue endpoints in app/api/routes_investigations.py) call serialize_lead once per lead in the investigation with no batching -- an investigation with a few hundred leads can turn one page load of the reporter's main queue view into 1,000+ queries. Pre-existing logic, moved verbatim by the routes.py split (not a regression from it), but now isolated in one small, easy-to-fix file.", "precise_locations": ["backend/app/services/leads.py:35", "backend/app/services/leads.py:55", "backend/app/services/leads.py:124", "backend/app/services/leads.py:206", "backend/app/services/investigations.py"], "required_correction": "Batch-load: fetch all LeadLink/ReportingTask/LeadWorkflowEvent rows for the whole investigation's lead set in a handful of `.in_()` queries up front, then group them in memory per lead instead of querying per lead inside a loop. Same treatment for _lead_triage's relationship-attachment/review-event lookups. Prioritize this over the other findings in this pass since it sits on the reporter's most-used screen and scales with investigation size.", "reviewer_type": "AI_ASSISTED", "status": "CORRECTED", "progress_note": "Fixed. Added list_leads_serialized(), _lead_triage_batch(), _resolve_link_labels()/_serialize_links_batch(), and serialize_tasks_batch() (with a shared _build_task_dict() helper) to app/services/leads.py. list_investigation_leads and lead_queue in app/services/investigations.py now call list_leads_serialized(db, rows) once for the whole lead set instead of serialize_lead(db, row) per lead in a loop; list_investigation_reporting_tasks batches serialize_task the same way. Each new batch function fetches every referenced table (LeadProfile, LeadLink, ReportingTask, LeadWorkflowEvent, Entity/Source/Claim/Evidence/RelationshipEdge for link labels, RelationshipEvidenceAttachment, RelationshipEvidenceReviewEvent, ClaimEvidenceLink, ReportingTaskWorkflowEvent) once per investigation-sized batch via .in_() queries, instead of once per lead/link/task -- so query count for a leads-list page no longer scales with the number of leads. serialize_lead/serialize_link/serialize_task/_lead_triage are kept unchanged (not deprecated) for their existing single-record call sites (routes_leads.py, routes_reporting_tasks.py, dossier.py, provenance.py, convert_lead, lead_provenance_snapshot) since those only ever serialize one record per request and aren't part of the N+1 pattern. Verified against the full 220-test suite (tests/test_lead_workflow.py and tests/test_relationship_evidence_review.py in particular, which exercise this exact serialization path) plus the 80% coverage floor -- all green, no behavior change to any endpoint's response shape."}
{"finding_id": "STRUCT-0018", "audit_object": "backend/app/api/routes_*.py", "audit_pass": "performance", "severity": "MATERIAL", "finding": "Every investigation-scoped list endpoint across all 16 route modules (documents, entities, leads, claims, sources, connector-findings, connector-runs, reporting-tasks, etc.) runs an unbounded `SELECT ... WHERE investigation_id = ...` with no limit/offset -- confirmed via a repo-wide grep for `.limit(` across app/api/routes_*.py, which found it used only by /search (Query(50, ge=1, le=200)) and the security-audit-log endpoints. Fine for now; becomes a real problem as an investigation accumulates months of documents/entities/findings, since every list call returns and serializes the entire table slice with no way for a client to page through it.", "precise_locations": ["backend/app/api/routes_investigations.py", "backend/app/api/routes_entities.py", "backend/app/api/routes_documents.py", "backend/app/api/routes_connector_findings.py"], "required_correction": "Add optional limit/offset (or cursor) query parameters to the investigation-scoped list endpoints, with a sane default page size, starting with the ones most likely to grow largest in practice (documents, connector-findings, entities).", "reviewer_type": "AI_ASSISTED", "status": "IN_PROGRESS", "progress_note": "Partially fixed. Added optional limit/offset query params (SQL-level LIMIT/OFFSET, not post-fetch slicing) to the three endpoints the finding named as priority: GET /investigations/{id}/documents, GET /investigations/{id}/entities, and GET /investigations/{id}/connector-findings -- app.services.investigations.list_investigation_documents/list_investigation_connector_findings and app.services.entities.list_entities all take limit: int | None = None, offset: int = 0 now, and the route handlers expose them as Query(ge=1, le=500) / Query(ge=0). Both default to None/0, i.e. unpaginated, unbounded -- identical to prior behavior -- so no existing caller (frontend or test) changes behavior until it opts in. New coverage: tests/test_investigation_list_pagination.py (4 tests) verifies unpaginated output is unchanged, that limit/offset slice the same order the unpaginated call returns, and that out-of-range params 422. Deliberately NOT done in this pass, and left as a follow-up: (1) a default page size when the client sends no limit -- required_correction asks for one, but silently capping every existing caller (the frontend has no pagination UI yet) would look like data loss rather than a fix, so it needs to land together with frontend paging controls, not as a backend-only change; (2) the other 13 investigation-scoped list endpoints (leads, leads/queue, sources, claims, reporting-tasks, connector-runs, evidence, relationships, graph, timeline) still have no limit/offset at all -- leads/sources/claims/reporting-tasks/connector-runs are the next-most-likely to grow large and are the natural next slice of this same pattern."}
{"finding_id": "STRUCT-0019", "audit_object": "backend/app/services/settings.py:207-229", "audit_pass": "testing", "severity": "MATERIAL", "finding": "save_connector_credential and delete_connector_credential (extracted this session, Stage E group 6) have zero test coverage -- confirmed via a real `--cov` run, not just import-time instrumentation. tests/test_connector_credentials.py only exercises the lower-level app/services/credentials.py store (set_secret/get_secret/remove_secret/credential_status) directly; nothing calls the PUT/DELETE /api/settings/connectors/{provider}/credential endpoints or these two wrapper functions, so their bad-provider->404 / blank-secret->400 / store-failure->500 mapping is entirely unverified by the test suite.", "precise_locations": ["backend/app/services/settings.py:207-229", "backend/app/api/routes_settings.py"], "required_correction": "Add tests hitting PUT/DELETE /api/settings/connectors/{provider}/credential (or calling save_connector_credential/delete_connector_credential directly) covering: unknown provider (404), blank/missing credential (400), and a forced set_secret/remove_secret failure (500) -- the three branches the functions were specifically written to distinguish.", "reviewer_type": "AI_ASSISTED", "status": "CORRECTED", "progress_note": "Fixed. Added tests/test_connector_credential_endpoints.py (7 tests) covering PUT/DELETE /api/settings/connectors/{provider}/credential end to end through the FastAPI TestClient (not just calling save_connector_credential/delete_connector_credential directly), exercising exactly the three branches these functions were written to distinguish: unknown provider -> 404, blank/missing credential -> 400, and a forced set_secret/remove_secret failure (via monkeypatch) -> 500. Also covers the success round trip (save then delete, both 200) and confirms the saved secret value never appears in the response body. Each test monkeypatches settings.connector_credentials_file to a pytest tmp_path so it never touches the real ./data/secrets/connectors.json. app/services/settings.py coverage on this range rose from 0% to fully covered (module coverage 70% -> 85%). Verified against the full 231-test suite (224 + 7 new) with the 80% coverage floor held (87.36%)."}
{"finding_id": "STRUCT-0020", "audit_object": "backend/app/core/authorization.py:200-226", "audit_pass": "api-routes", "severity": "NON_MATERIAL", "finding": "In the default single-user 'unrestricted' authorization scope, authorize_routed_resource (the shared dependency applied to every route in every module) returns immediately without any existence check on investigation_id. Most list endpoints do their own `db.get(Investigation, investigation_id) is None` check and return 404 for a bad id, but list_sources, list_claims, list_connector_runs, and list_connector_findings (app/api/routes_investigations.py) skip that check -- so in unrestricted mode they silently return 200 [] for a nonexistent investigation_id instead of 404, unlike their sibling list endpoints in the same file. No security impact (unrestricted mode already grants full access), just an inconsistency.", "precise_locations": ["backend/app/api/routes_investigations.py"], "required_correction": "Add the same `if db.get(Investigation, investigation_id) is None: raise HTTPException(404, ...)` check to list_sources, list_claims, list_connector_runs, and list_connector_findings, matching their sibling endpoints in the same module.", "reviewer_type": "AI_ASSISTED", "status": "CORRECTED", "progress_note": "Fixed. Added the same `if db.get(Investigation, investigation_id) is None: raise HTTPException(404, ...)` check to list_sources, list_claims, list_connector_runs, and list_connector_findings, matching their sibling list endpoints in the same module. Added tests/test_investigation_list_404_consistency.py (2 tests) covering both directions: all 4 endpoints now 404 for a nonexistent investigation_id, and still correctly return 200 [] for a real investigation with no rows (not a regression to a different failure mode). Verified via the full backend suite (240 tests, all passing, exit 0)."}
{"finding_id": "STRUCT-0021", "audit_object": "backend/app/api/routes_documents.py:30-95", "audit_pass": "testing", "severity": "NON_MATERIAL", "finding": "routes_documents.py is the lowest-covered route module after the split (49%, vs. 75-100% for the other 15). The uncovered lines are almost entirely the OpenAleph corpus sync/review/import endpoints (get_openaleph_document_sync, get_openaleph_document_review_status, sync_openaleph_document, refresh_openaleph_document_review, import_openaleph_document_evidence, import_openaleph_document_entities), which need the external OpenAleph service mocked to exercise meaningfully -- likely why they were left thin. Pre-existing gap, not introduced by the split.", "precise_locations": ["backend/app/api/routes_documents.py:30-95"], "required_correction": "Add tests that mock the OpenAleph client boundary (similar to how app/services/openaleph.py's probe_openaleph is already mocked in tests/test_openaleph_integration.py) and exercise each of these six endpoints' success and failure paths.", "reviewer_type": "AI_ASSISTED", "status": "CORRECTED", "progress_note": "Fixed. Added tests/test_openaleph_document_routes_coverage.py (17 tests) exercising the ROUTE layer of all 6 named endpoints plus get_document/list_document_candidates_endpoint/upload_document alongside them -- their 404/409/503/success paths were previously only exercised at the service layer (tests/test_openaleph_corpus_bridge.py calls sync_document_to_openaleph/ensure_openaleph_collection directly, never through HTTP). Followed the finding's suggested mocking boundary in spirit: rather than mocking the OpenAleph HTTP client itself (already covered thoroughly at the service layer via client_factory injection), monkeypatched each service function at the route-module import site (the pattern tests/test_openaleph_operation_failures.py already established, since route modules call these as bare names from their own namespace) so each test isolates and exercises only the route handler's own try/except/response-shaping logic. routes_documents.py coverage rose from 72% (already improved from the finding's original 49% by earlier STRUCT-0027/0028 work) to 99% (one bare `raise` statement inside a re-raise branch not counted by coverage tooling). Writing the extraction-failure test surfaced a real, previously-undiscovered bug along the way: upload_document's 422 detail (`HTTPException(422, {..., \"document\": serialize_document(db, doc), ...})`) embeds a raw `datetime` field, which FastAPI's default HTTPException handler cannot JSON-encode (it does not run jsonable_encoder the way a normal 200 response does) -- meaning a real extraction failure in production would have crashed with an unhandled 500 instead of returning the intended 422. Fixed by wrapping that one HTTPException's detail in fastapi.encoders.jsonable_encoder(...). Verified via the full backend suite (257 tests, all passing, exit 0)."}
{"finding_id": "STRUCT-0022", "audit_object": "backend/app/connectors/registry.py", "audit_pass": "architecture", "severity": "MATERIAL", "finding": "ConnectorRegistry._build() hardcodes exactly two providers ('aleph', 'opensanctions') via an if/elif chain reading provider-specific settings/secrets, keyed off a names()/refresh() literal list of those same two names. register() exists on the class but nothing in the codebase calls it -- it's dead code, not an actual extension point. Adding any new connector means editing _build() and refresh()'s literal list, not registering a provider against a config-driven table. Flagged now specifically because Ony's next planned step is a Firecrawl connector; adding it under the current shape means the registry grows into a longer if/elif chain rather than becoming more general. CORRECTED (commit 228eec1): ConnectorRegistry now iterates a CONNECTOR_SPECS provider table; app/services/settings.py's CONNECTOR_PROVIDERS and connector_credential_status derive from the same table. Full suite: 214 passed.", "precise_locations": ["backend/app/connectors/registry.py"], "required_correction": "Generalize ConnectorRegistry to a small provider table (name -> connector class + required settings/secret keys) that refresh()/names() iterate over, so adding Firecrawl (or any future connector) is a table entry plus a Connector subclass, not an edit to _build()'s if/elif chain. Do this as part of, or immediately before, the Firecrawl integration work rather than as a separate speculative refactor.", "reviewer_type": "AI_ASSISTED", "status": "CORRECTED"}
{"finding_id": "STRUCT-0023", "audit_object": "backend/app/main.py", "audit_pass": "architecture", "severity": "OPTIONAL", "finding": "Two authorization paths coexist in the api_access_guard middleware: a single shared static bearer token (settings.api_auth_token, optionally scoped to a fixed investigation-id allowlist via api_auth_investigation_ids) checked by enforce_api_access, and a newer per-user persisted-token system (AppUser + InvestigationMembership, real viewer/reporter/admin roles, added in this session's Stage E group 6 settings split) resolved by scope_for_persisted_token and tried first. The docstring on AuthorizationScope ('the local single-user workstation remains unrestricted; remote shared-bearer access can be narrowed with JW_API_AUTH_INVESTIGATION_IDS') shows this is a deliberate in-progress migration, not an oversight -- but no README/ADR states which path is meant to be primary going forward or whether/when the shared-token path is meant to be retired once per-user tokens are considered solid.", "precise_locations": ["backend/app/main.py", "backend/app/core/authorization.py", "backend/app/services/identity.py"], "required_correction": "Write a short ADR (or a README/SECURITY.md section) stating the intended end-state for these two auth paths -- e.g. shared token stays as a local-dev/single-user convenience while persisted per-user tokens become the only path for remote/shared access, with a target condition for removing api_auth_token entirely -- so future changes to either path are made against a stated intent rather than inferred from the middleware's fallback order.", "reviewer_type": "AI_ASSISTED", "status": "CORRECTED", "progress_note": "Fixed. Wrote docs/adr/0002-authorization-two-tier-end-state.md after tracing the actual middleware code (app/main.py's api_access_guard, app/core/authorization.py, app/services/identity.py) rather than guessing from the docstring alone. Two facts from that reading shaped the decision: (1) local requests bypass both auth paths entirely (request_is_local_request short-circuits the whole block), so neither path is involved in ordinary single-workstation use; (2) the two remote paths are already fully independent, not layered -- a persisted per-user token grants access even when api_auth_token is unset (the default, which fails closed for the shared-token path alone). Given that independence, the ADR proposes treating them as permanently coexisting tiers rather than inventing a migration deadline: persisted per-user tokens (viewer/reporter/admin roles, individually revocable) are the recommended path for any multi-person or role-differentiated remote access and the only path future remote-auth features should extend; the shared JW_API_AUTH_TOKEN stays as a deliberately minimal, feature-frozen convenience for a solo operator or script, with no stated removal condition since nothing depends on retiring it. Also added a short pointer to the ADR in AuthorizationScope's own docstring (app/core/authorization.py) so a future reader lands on the stated intent instead of re-deriving it from the fallback order. Marked Proposed, pending Ony's review, matching this repo's existing ADR-0001 convention. Docs-only change; no backend logic touched beyond the docstring."}
{"finding_id": "STRUCT-0024", "audit_object": "backend/app/models/domain.py", "audit_pass": "architecture", "severity": "NON_MATERIAL", "finding": "All 39 SQLAlchemy models live in one 537-line file. Not large enough to be a problem today, but this is the same shape app/api/routes.py had before this session's Stage E split (which grew from a reasonable size to 1859 lines/120 endpoints before anyone treated it as a problem). Flagging now, while the split methodology used for routes.py/services this session is still fresh, as a marker to split models/domain.py by domain (mirroring the 16 route/service module boundaries already established) before it reaches a similar size, rather than after.", "precise_locations": ["backend/app/models/domain.py"], "required_correction": "No action needed now. Revisit if models/domain.py grows meaningfully past its current 537 lines -- split by the same domain boundaries as app/api/routes_*.py / app/services/*.py (investigations, entities, leads, documents, connectors, settings, etc.), each domain's models in their own module, all still declared against the shared Base from app/db/session.py.", "reviewer_type": "AI_ASSISTED", "status": "OPEN", "progress_note": "Reviewed this cycle (not corrected -- this finding's own required_correction says no action is needed unless the file grows meaningfully past its recorded 537 lines). Current count: backend/app/models/domain.py is now 558 lines / 42 classes (up from the recorded 537 lines / 39 classes -- the growth is from STRUCT-0027's OpenAlephOperationFailure model added earlier this session, plus incidental additions). ~4% growth is not 'meaningfully past' the threshold this finding sets for action. Left status OPEN (matching the STRUCT-0031/STRUCT-0035 watch-item pattern) rather than marking CORRECTED with no real split performed. Re-check the line/class count again next time this finding comes up for review, and perform the domain-based split (mirroring app/api/routes_*.py / app/services/*.py's 16 module boundaries, all still declared against the shared Base from app/db/session.py) once it has."}
{"finding_id": "STRUCT-0025", "audit_object": "backend/app/ai/tas_spec (submodule) and backend/app/ai/reasoning.py", "audit_pass": "architecture", "severity": "NON_MATERIAL", "finding": "The TAS methodology spec is vendored as a git submodule (backend/app/ai/tas_spec, pinned at v1.8.0 of onysnow/topic-authority-system) rather than copied in-repo. Good boundary -- CI correctly tolerates a missing submodule on Dependabot PRs rather than failing outright -- but only 2 of TAS's modules (case_synthesis, hypothesis_test) are currently wired to real LLM-backed endpoints in app/ai/reasoning.py, out of what PROMPT_MODULE_CATALOG.md in the submodule suggests is a larger catalog. There is no automated check for submodule drift (a pinned commit that has fallen behind the upstream TAS repo, or an upstream module rename/restructure that would silently break MODULE_FILES' path lookups) and no documented process for bumping the pin.", "precise_locations": ["backend/app/ai/tas_spec", "backend/app/ai/reasoning.py"], "required_correction": "As more TAS modules get wired up as real endpoints, add a lightweight CI check (or at minimum a documented manual step) that verifies the pinned submodule commit still contains every MODULE_FILES path app/ai/reasoning.py references, so an upstream TAS restructure fails CI instead of failing silently at request time with a FileNotFoundError.", "reviewer_type": "AI_ASSISTED", "status": "CORRECTED", "progress_note": "Fixed (the lightweight-check option, per required_correction). Added backend/tests/test_tas_submodule_drift.py: it walks reasoning.MODULE_FILES and asserts every path resolves to a real, non-empty file inside the pinned tas_spec submodule commit -- so an upstream TAS restructure (a module renamed or moved) now fails this test with a clear assertion instead of failing silently at request time with a FileNotFoundError. Deliberately skips (not fails) when tas_spec isn't checked out at all, exactly mirroring .github/workflows/backend-postgres-ci.yml's own tolerance of a missing submodule on Dependabot PRs -- so this doesn't turn that tolerated case into a new CI failure. Also added a short pointer comment directly above MODULE_FILES in app/ai/reasoning.py so anyone bumping the submodule pin or wiring up a new TAS module knows to run this test first. Did not add a new CI workflow step (the existing pytest job already runs this test as part of the full suite whenever the submodule fetch step succeeds), and did not attempt the larger 'wire up more TAS modules' work implied by the finding's context -- that's tracked separately and isn't what required_correction actually asked for. Verified via the full backend suite (258 tests, all passing, exit 0)."}
{"finding_id": "STRUCT-0026", "audit_object": "backend/requirements.txt", "audit_pass": "dependencies", "severity": "NON_MATERIAL", "finding": "pytest and pytest-cov are listed directly in backend/requirements.txt -- the same file backend/Dockerfile installs from for the runtime image -- rather than a separate dev-only requirements file or extras group. Minor image-bloat/attack-surface nit: the production container ships test tooling it never runs.", "precise_locations": ["backend/requirements.txt"], "required_correction": "Split backend/requirements.txt into a runtime file and a requirements-dev.txt (or a pip extras group) that adds pytest/pytest-cov only for CI and local development; update backend/Dockerfile and the CI workflows' install steps accordingly.", "reviewer_type": "AI_ASSISTED", "status": "OPEN"}
{"finding_id": "STRUCT-0027", "audit_object": "backend/app/api/routes_documents.py, backend/app/api/routes_investigations.py", "audit_pass": "debug", "severity": "MATERIAL", "finding": "The 4 OpenAleph pipeline endpoints that catch bare `except Exception` (refresh_openaleph_document_review, import_openaleph_document_evidence, import_openaleph_document_entities in routes_documents.py; ensure_openaleph_corpus_binding in routes_investigations.py) leave no server-side record once a failure happens. Unlike app/services/connectors.py's run_connector/enrich_entity, which persist run.error onto the ConnectorRun row before re-raising, there is no equivalent table for OpenAleph operations, and the security audit log (app/core/audit_log.py) only records method/path/status_code, never error text. So when an OpenAleph sync/import call fails, the only trace of *why* is whatever the caller happened to see in that one HTTP response -- there is no way to look it up afterward (a flaky remote OpenAleph instance, a background retry, or a bug report arriving days later all hit this gap the same way).", "precise_locations": ["backend/app/api/routes_documents.py:66", "backend/app/api/routes_documents.py:79", "backend/app/api/routes_documents.py:92", "backend/app/api/routes_investigations.py:75"], "required_correction": "Give OpenAleph operations the same treatment run_connector/enrich_entity already get: persist the failure (document_id or investigation_id, operation name, exception text, timestamp) somewhere queryable -- either a small dedicated table or, at minimum, a new audit_log event type -- before re-raising as the 502.", "reviewer_type": "AI_ASSISTED", "status": "CORRECTED", "progress_note": "Fixed. Added a new OpenAlephOperationFailure table (migration 48d964619dd5_add_openaleph_operation_failures.py, verified via alembic upgrade/downgrade and `alembic check` reporting no diff against the ORM metadata) with columns investigation_id, document_id (nullable -- collection setup can fail before any InvestigationCorpusBinding row exists), operation, error, created_at. Added record_openaleph_operation_failure()/list_openaleph_operation_failures()/serialize_operation_failure() to app/services/openaleph_corpus.py. All 4 flagged endpoints (refresh_openaleph_document_review, import_openaleph_document_evidence, import_openaleph_document_entities in routes_documents.py; ensure_openaleph_corpus_binding in routes_investigations.py) now roll back the session and record a failure row -- with the document's investigation_id, or the path investigation_id and a null document_id for collection setup -- before re-raising the 502, exactly the same shape run_connector/enrich_entity already give connector failures. Added GET /investigations/{id}/corpus/openaleph/failures so these are actually queryable, not just present in a table nobody can reach. Deliberately did NOT touch sync_document_to_openaleph -- it already records failures onto its own DocumentCorpusSync.error field and was correctly excluded from this finding's precise_locations. Added tests/test_openaleph_operation_failures.py (6 tests): each of the 4 endpoints' failure path is triggered by monkeypatching the service call at the route module's import site (necessary because these are looked-up-by-name module globals, not bound default arguments) to force a bare Exception, then confirms the recorded row's operation/document_id/investigation_id and that the new endpoint returns it, plus per-investigation scoping and 404 on an unknown investigation. Verified against the full 237-test suite (231 + 6 new) and the 80% coverage floor (87.77%)."}
{"finding_id": "STRUCT-0028", "audit_object": "backend/app/api/routes_documents.py, backend/app/api/routes_investigations.py, backend/app/services/connectors.py", "audit_pass": "debug", "severity": "OPTIONAL", "finding": "6 call sites (routes_documents.py x3, routes_investigations.py x1, services/connectors.py x2) build their 502 HTTPException detail as an f-string embedding the raw exception text (3 of them also include exc.__class__.__name__), putting internal/external-service failure text directly into the API response body. All are calls into external services (OpenAleph, aleph, opensanctions), so the risk is contained today under the single-user/trusted-caller default, but it is a consistent pattern across 6 sites, not a one-off, and would matter more the moment these responses reach a lower-trust caller -- relevant given STRUCT-0023's note that per-user remote access is an in-progress direction for this app.", "precise_locations": ["backend/app/api/routes_documents.py:67", "backend/app/api/routes_documents.py:80", "backend/app/api/routes_documents.py:93", "backend/app/api/routes_investigations.py:76", "backend/app/services/connectors.py:76", "backend/app/services/connectors.py:98"], "required_correction": "Log the full exception (message + class) server-side -- ideally as part of the STRUCT-0027 fix -- and return a generic client-facing message plus the request_id already set on every response (main.py's X-Request-ID header) so a caller can still cross-reference without the raw internals traveling in the response body.", "reviewer_type": "AI_ASSISTED", "status": "CORRECTED", "progress_note": "Fixed at all 6 sites. The 4 OpenAleph route-handler sites (routes_documents.py x3, routes_investigations.py x1) already log the full exception server-side as part of the STRUCT-0027 fix (record_openaleph_operation_failure persists error class + message); this pass changed their 502 detail from the raw f-string to a generic message plus a cross-reference id, using the request's own request_id -- main.py's api_access_guard middleware now stashes it on request.state.request_id (previously computed but never exposed past the middleware closure) so route handlers can reference the exact id that also appears in the X-Request-ID response header and the security audit log, without threading a new id-generation scheme through the app. The 2 services/connectors.py sites (run_connector, enrich_entity) don't have a Request object at all -- they're plain service functions, not route handlers, and both already persist the full exception on the ConnectorRun row (run.error) before this fix. Since there's no existing endpoint to look up a ConnectorRun by id yet, used the run's own id as the cross-reference token instead of threading a request_id through routes_connectors.py/routes_entities.py's call sites into these service functions, which would have been a much wider, riskier change for an OPTIONAL finding -- the run id at least ties the response to a specific persisted, queryable row for direct DB inspection today, and remains available to expose through an API once one exists. Verified via the full backend suite (238 tests, all passing unchanged, exit 0); confirmed no test asserts on the removed raw-exception detail text."}
{"finding_id": "STRUCT-0029", "audit_object": "backend/Dockerfile, frontend/Dockerfile, docker-compose.yml", "audit_pass": "deploy-checklist", "severity": "MATERIAL", "finding": "Neither Dockerfile declares a HEALTHCHECK, and docker-compose.yml gives the `backend`/`frontend` services no healthcheck either -- even though app/api/routes_system.py already exposes GET /health. Contrast with workbench-db and the openaleph-profile postgres, both of which have a real `pg_isready` healthcheck and `condition: service_healthy` gating. A hung or crash-looping backend container (e.g. stuck on a slow OpenAleph probe at startup) is invisible to Docker and to `frontend`'s `depends_on: [backend]` (which only waits for container start, not readiness) -- Docker will not restart it and nothing waits for it to actually become healthy before the frontend starts.", "precise_locations": ["backend/Dockerfile", "frontend/Dockerfile", "docker-compose.yml"], "required_correction": "Add a HEALTHCHECK to both Dockerfiles (curl/wget against GET /health for the backend; a simple TCP or HTTP check for the frontend), and wire docker-compose.yml's `frontend.depends_on.backend` to `condition: service_healthy`, matching the pattern already used for workbench-db.", "reviewer_type": "AI_ASSISTED", "status": "CORRECTED", "progress_note": "Fixed. Added a HEALTHCHECK to both Dockerfiles: backend uses `python -c \"...urllib.request.urlopen('http://localhost:8000/api/health')...\"` (no new package -- GET /api/health is auth-exempt for loopback callers per app.core.access.request_is_local_request, confirmed by a new tests/test_health_endpoint.py check, so the in-container healthcheck needs no API_AUTH_TOKEN); frontend uses `node -e \"require('http').get(...)\"` against the root page (no dedicated frontend health route exists, and adding one was out of scope). Both avoid adding curl/wget as a new image dependency by using the runtime already in the base image. docker-compose.yml's frontend.depends_on.backend now uses `condition: service_healthy` instead of the bare list form (which only waited for container start, not readiness), matching the pattern workbench-db/postgres already use. Not locally build-verified -- this sandbox has no docker binary -- but .github/workflows/docker-build.yml already runs `docker compose build backend frontend` on every push, which will catch any Dockerfile syntax error; watching that run after push is the verification step for this specific fix. Added tests/test_health_endpoint.py (1 test) confirming GET /api/health returns 200 for an unauthenticated local-style request (matching how the healthcheck actually calls it), which nothing previously verified. Verified against the full 238-test suite (237 + 1 new) and the 80% coverage floor (87.79%)."}
{"finding_id": "STRUCT-0030", "audit_object": "frontend/Dockerfile", "audit_pass": "deploy-checklist", "severity": "NON_MATERIAL", "finding": "frontend/Dockerfile runs `npm install`, while .github/workflows/frontend-ci.yml runs `npm ci` (deterministic install strictly from package-lock.json). The two install commands can resolve different dependency versions when the lockfile and package.json have drifted even slightly, meaning the image that gets built and run is not guaranteed to match what CI actually tested against.", "precise_locations": ["frontend/Dockerfile"], "required_correction": "Change `RUN npm install` to `RUN npm ci` in frontend/Dockerfile so the built image installs from the same lockfile-pinned versions CI verifies.", "reviewer_type": "AI_ASSISTED", "status": "OPEN"}
{"finding_id": "STRUCT-0031", "audit_object": "docker-compose.yml", "audit_pass": "deploy-checklist", "severity": "OPTIONAL", "finding": "docker-compose.yml is the only deployment artifact in the repo, and it is a local-dev configuration, not a production one: `backend` bind-mounts `./backend:/app` over the image's own `COPY . .` layer (so the container actually runs whatever is on the host filesystem, not what was baked into the image), both `backend`/`frontend` ports are bound to 127.0.0.1 only, and API_AUTH_TOKEN defaults to the literal string `docker-compose-local-dev-token`. .github/workflows/docker-build.yml only runs `docker compose build` -- it never pushes an image anywhere -- and there is no separate production compose file, deployment manifest, or documented rollback runbook alongside the 19-migration Alembic chain. Consistent with a local-first single-user tool (not faulting the absence of hosted-deploy infrastructure), but worth stating explicitly so a future 'let's put this on a shared server' moment doesn't reach for this file assuming it is deploy-ready.", "precise_locations": ["docker-compose.yml", ".github/workflows/docker-build.yml"], "required_correction": "No action needed while this stays a local-first single-user tool. If/when shared or hosted deployment is ever wanted, add a separate production compose file or manifest (no source bind-mount, real secrets, no 127.0.0.1-only binding) and a short rollback runbook (how to run `alembic downgrade`, restore from the existing backup/export feature) rather than adapting this dev-oriented file in place.", "reviewer_type": "AI_ASSISTED", "status": "OPEN", "progress_note": "Reviewed this cycle, no code change made or needed: required_correction itself says 'no action needed while this stays a local-first single-user tool,' and it still does. Re-checked docker-compose.yml directly -- the bind mount (`./backend:/app` over the image's own COPY layer), the 127.0.0.1-only port bindings, and the API_AUTH_TOKEN default of 'docker-compose-local-dev-token' are all still exactly as described; no separate production compose file or manifest has been added. Leaving OPEN (not CORRECTED, since nothing was actually changed) as a genuine watch item: revisit when/if hosted or shared-server deployment is actually planned, per the finding's own trigger condition."}
{"finding_id": "STRUCT-0032", "audit_object": "README.md", "audit_pass": "documentation", "severity": "MATERIAL", "finding": "README.md's 'Current status' table lists three rows as 'In progress' against open PRs -- Postgres CI (PR #5), Docker build/boot (PR #6), and frontend production build/typecheck (PR #4) -- but git history shows all three already merged, each followed by further stabilization commits well after the merge (e.g. 'Fix Docker compose build/boot for the workbench stack (#6)', 'Stabilize frontend dependency/build chain and add path-scoped frontend CI (#4)', plus multiple post-merge Postgres CI hotfixes including today's STRUCT-0006 single-head guard). The table's last edit was itself the PR #6 merge commit, so the content wasn't updated to reflect what that same commit was merging. A reader following this table today would believe core CI/Docker work is still unfinished when it has been running in CI for some time.", "precise_locations": ["README.md"], "required_correction": "Update the 'Current status' table: move Postgres CI, Docker build/boot, and frontend production build/typecheck out of 'In progress' (drop the PR links or replace with 'Validated -- running in CI' the way the first row already reads), keeping only genuinely still-open items (e.g. the dependency security posture row, if issue #20 is still open) marked in-progress.", "reviewer_type": "AI_ASSISTED", "status": "CORRECTED", "progress_note": "Fixed. Confirmed via git merge-base --is-ancestor that all three referenced commits (371b622 PR #5, 90cf4d1 PR #6, 2e5411e PR #4) are ancestors of the current HEAD, and that the workflows they added still exist (backend-postgres-ci.yml, docker-build.yml). Updated README.md's 'Current status' table: all three rows now read 'Validated -- running in CI' (linking the actual workflow file instead of the merged PR), matching the first row's existing style, and updated the closing sentence from 'in the linked PRs above' to 'in CI on every push'. Left the 'Dependency security posture' (issue #20) row unchanged -- I have no way to check GitHub's live issue state from this sandbox (no gh CLI, no GitHub API access for this repo), and the finding's own required_correction only asks to update that row 'if' the issue is confirmed still open, so leaving an unverifiable claim alone is the safer default. requirements.txt/package.json versions (next 15.5.25, python-multipart 0.0.32, pypdf 6.16.1) suggest the specific CVEs named in that row may already be patched, but that's a different question from whether issue #20 itself is closed -- left for whoever can check GitHub directly."}
{"finding_id": "STRUCT-0033", "audit_object": "CHANGELOG.md, README.md, dev-log.md", "audit_pass": "documentation", "severity": "MATERIAL", "finding": "README.md points readers at CHANGELOG.md as 'a detailed dev log', but CHANGELOG.md's last entry (DEV 0.93) was committed 2026-09-16 as part of PR #22 ('Rewrite README as a product-first overview; move dev journal to CHANGELOG.md') and hasn't been touched since. Every subsequent unit of work -- the entire Stage E routes.py split (8 groups, 120 endpoints moved into 16 domain modules) and this session's engineering-skills audit (code-review, architecture, debug, deploy-checklist passes, 31 STRUCT-00xx findings so far) -- was recorded only in dev-log.md, a file README.md never mentions. A reader who follows README's own pointer to 'see CHANGELOG.md' for the dev journal would see a project that looks to have stalled on 2026-09-16, missing everything since.", "precise_locations": ["CHANGELOG.md", "README.md", "dev-log.md"], "required_correction": "Either resume appending to CHANGELOG.md (even a compact entry per work session) so README's pointer stays accurate, or update README.md to point at dev-log.md (or both, if they're meant to serve different audiences -- CHANGELOG.md as product-facing history, dev-log.md as the working engineering log) and say so explicitly rather than leaving the split implicit.", "reviewer_type": "AI_ASSISTED", "status": "CORRECTED", "progress_note": "Fixed, taking the 'both, made explicit' option the finding itself suggested. Added a CHANGELOG.md entry ('Engineering hardening pass (post-DEV 1.24)') summarizing at a product level everything since DEV 0.93/1.24 -- the routes.py Stage E split, the engineering-skills audit and STRUCTURE_AUDIT.md's 40 tracked findings, the Firecrawl connector -- and explicitly pointing to dev-log.md for full session-by-session detail, rather than trying to write one CHANGELOG entry per STRUCT-00xx fix (that granularity is what dev-log.md is for). Updated both of README.md's CHANGELOG.md pointers to also name dev-log.md and say what each file is for, so the split is explicit instead of implicit. Also fixed a related staleness bug discovered while researching this: dev-log.md's own header still described 'BUILD_STATE.md' as the product changelog, but BUILD_STATE.md was superseded by CHANGELOG.md in PR #22 (2026-09-16) and hasn't been touched since -- confirmed via git log; it's now a frozen historical artifact, not a maintained file. Corrected dev-log.md's header to reference CHANGELOG.md instead. Docs-only change; no backend tests affected."}
{"finding_id": "STRUCT-0034", "audit_object": "SECURITY.md", "audit_pass": "incident-response", "severity": "MATERIAL", "finding": "There is no incident-response runbook for this project's actual operator. SECURITY.md only covers how an outside contributor reports a vulnerability privately -- it says nothing about what Ony (the single trusted operator) should do if the security audit log shows unexpected access_denied/auth_rate_limited spikes, a connector credential is suspected leaked, or investigation data looks tampered with. The individual pieces needed for this already exist and are well built (SecurityAuditLogger's read/summarize/retention-preview functions in app/core/audit_log.py, full backup export/inspect/restore in app/services/exports.py, connector-credential rotation via app/services/settings.py) but nothing ties them together into an actual 'if you see X, do Y, in this order' procedure.", "precise_locations": ["SECURITY.md"], "required_correction": "Add a short operator-facing incident-response section (to SECURITY.md or a new INCIDENT_RESPONSE.md): how to pull and read the security audit log for a given window, how to rotate a suspected-leaked connector credential, and how to take a fresh backup export before investigating further so evidence of the incident itself isn't lost.", "reviewer_type": "AI_ASSISTED", "status": "CORRECTED", "progress_note": "Fixed. Added a new operator-facing INCIDENT_RESPONSE.md covering: (1) preserve evidence first via per-investigation export (GET /investigations/{id}/export) before touching anything else; (2) pull and read the security audit log for the window in question -- GET /settings/security/audit/summary for aggregate counts, GET /settings/security/audit filtered by event/status_code for the actual records, covering the real event names (access_denied, auth_rate_limited, request_rejected, browser_write_denied); (3) rotate a suspected-leaked connector credential via PUT/DELETE /settings/connectors/{provider}/credential; (4) manual guidance for suspected data tampering (no dedicated diff tool exists today -- documented as a real, honest limitation rather than papered over); and an 'after the incident' section on pruning the audit log, which correctly states that POST /settings/security/audit/retention requires the exact literal confirmation string 'PRUNE SECURITY AUDIT' (verified directly against apply_security_audit_retention in app/core/audit_log.py). Left SECURITY.md's existing contributor-facing vulnerability-disclosure content unchanged and added one short 'Operator incident response' section pointing to the new file, since the two audiences (external reporters vs. the operator) are distinct. Docs-only change; no backend tests affected."}
{"finding_id": "STRUCT-0035", "audit_object": "backend/app/core/audit_log.py, backend/app/core/rate_limiter.py", "audit_pass": "incident-response", "severity": "OPTIONAL", "finding": "Both the security audit log and the auth-failure rate limiter are purely passive/in-process: nothing surfaces a spike in access_denied or auth_rate_limited events to the operator proactively (no notification, digest, or 'recent failures' surface anywhere in the settings UI/API) -- the log only gets read if someone thinks to ask for it. FixedWindowRateLimiter's own docstring already self-documents that its counters are in-memory and reset on process restart, and are 'intentionally best-effort for the current single-user/local-first runtime.' Fine for a private single-workstation tool where the owner is the only one ever likely to hit these paths, but worth naming now given STRUCT-0023's noted direction toward per-user remote access, where 'nobody would notice a brute-force attempt unless they went looking for it' becomes a materially bigger gap.", "precise_locations": ["backend/app/core/audit_log.py", "backend/app/core/rate_limiter.py"], "required_correction": "No urgent action while remote/shared access remains experimental. If/when it becomes a real deployment mode, add at least a passive surface (e.g. a settings-panel widget showing recent access_denied/auth_rate_limited counts) so an operator can notice a problem without proactively querying the audit log, and consider persisting rate-limiter state so a restart doesn't reset an in-progress lockout.", "reviewer_type": "AI_ASSISTED", "status": "OPEN", "progress_note": "Reviewed this cycle, no code change made or needed: required_correction itself says 'no urgent action while remote/shared access remains experimental.' Re-checked against this session's STRUCT-0023 ADR (docs/adr/0002-authorization-two-tier-end-state.md) -- persisted per-user tokens are a complete, independent auth path today, but there is still no stated product move toward actual multi-person shared/remote deployment as the norm, so the audit-log/rate-limiter passivity this finding flags remains an accepted trade-off rather than an active gap. Leaving OPEN (not CORRECTED) as a genuine watch item: revisit -- add a passive settings-panel surface for recent access_denied/auth_rate_limited counts, and consider persisting rate-limiter state -- once remote/shared access moves from 'available' to 'actually used by more than the owner.'"}
{"finding_id": "STRUCT-0036", "audit_object": "backend/app/services/search.py", "audit_pass": "system-design", "severity": "MATERIAL", "finding": "investigation_search() -- which backs both the standalone /search endpoint and, via build_question_context(), every AI assistant question (case-synthesis, hypothesis-test) -- fetches every row of every searchable model type for the investigation (entities, statements for every one of those entities, sources, claims, leads, connector findings, relationships, timeline events, reporting tasks, documents) into Python with no SQL-level text filtering at all, then scores every single row with _score() in Python before sorting and truncating to `limit`. Confirmed by reading the fetch code directly (`db.scalars(entities_stmt).all()` with only an investigation_id predicate, then a full Python loop scoring every entity; the same pattern repeats for statements, sources, etc.). Matching entities additionally call resolve_active_entity() one at a time (an N+1-shaped cost on top of the full scan). This is a deliberate, documented trade-off for cross-dialect score-ordering consistency between SQLite and PostgreSQL (see _score()'s docstring), not an oversight -- but the consequence is that every search call's cost scales with an investigation's total record count (or, in an unrestricted global search with no investigation_id, every investigation's total record count combined), not with how many results actually match.", "precise_locations": ["backend/app/services/search.py"], "required_correction": "Add a cheap SQL-level prefilter ahead of the Python scorer -- a portable LIKE/ILIKE predicate at minimum, or a real per-dialect full-text index (SQLite FTS5, PostgreSQL tsvector) if ordering consistency can be preserved -- so only plausible candidate rows reach _score(), and batch resolve_active_entity() lookups for the matched entity set instead of calling it per row. Worth prioritizing as investigations grow into the thousands-of-records range this tool is explicitly built to support (per README's own framing).", "reviewer_type": "AI_ASSISTED", "status": "IN_PROGRESS", "progress_note": "Partial fix. Found and eliminated 3 genuine N+1s that were compounding the full-scan cost on every single search call, regardless of whether the parent record matched: (1) LeadProfile was queried once per lead in the investigation (now batch-loaded with one IN() query keyed by lead id); (2) DocumentChunk and (3) ExtractionCandidate were each queried once per document in the investigation (now batch-loaded with one IN() query each, keyed by document id, mirroring the batching pattern Statement already used for entity_id). Verified behaviorally identical -- full backend suite (268 tests) passes unchanged, including test_search_provenance.py's ranking and provenance-trace assertions. resolve_active_entity()'s per-row db.get() calls, and the inline db.get(Entity, ...) calls in the relationship loop, were investigated and found to already be effectively free: every Entity for the investigation is loaded into the session up front via entities_stmt, so SQLAlchemy's identity map answers those get()s without a second query -- no fix needed there. Deliberately NOT yet fixed: the core 'full in-memory scan-and-score' issue the finding is really about -- adding a SQL-level prefilter (LIKE/ILIKE or FTS) ahead of _score() so non-matching rows are never fetched at all. Attempting this exposed a real correctness hazard that needs a proper design pass rather than a quick patch: entities_stmt and sources_stmt are not just used to score 'entity'/'source' type hits -- entity_by_id and source_by_id built from their FULL, unfiltered results are reused to look up and score statements/evidence/documents whose OWN text can match the query even when their parent entity or source's fields do not. Naively adding a prefilter to entities_stmt/sources_stmt would silently drop those statement/evidence/document matches from search results -- a worse bug than the performance issue itself. A correct fix needs to split 'which entities/sources exist in scope' (cheap, ID-only, unfiltered) from 'which entities/sources themselves match the query' (prefiltered, for entity/source-type hits only), which is more invasive than this pass's time budget allows to do carefully. Filed as a dedicated follow-up task rather than rushed."}
{"finding_id": "STRUCT-0037", "audit_object": "backend/Dockerfile, backend/app/db/locking.py", "audit_pass": "system-design", "severity": "OPTIONAL", "finding": "The concurrency-control design (app/db/locking.py's PostgreSQL advisory locks, app/db/mutation_guard.py's before_flush guard) is genuinely solid and correctly gates itself to the postgresql dialect, explicitly no-op on SQLite with a comment explaining why. backend/Dockerfile's CMD runs uvicorn with no --workers flag (single process today), which is consistent with that design, but nothing documents the constraint this implies: a SQLite-backed deployment must never run multiple uvicorn worker processes (or multiple container replicas) against the same database file, since SQLite's locking model isn't what the advisory-lock coordination assumes and concurrent writers would race or corrupt the file. PostgreSQL deployments are correctly built to scale this way; SQLite ones are not, and that asymmetry isn't written down anywhere.", "precise_locations": ["backend/Dockerfile", "backend/app/db/locking.py"], "required_correction": "Add a one-line note near DATABASE_URL/SQLite configuration (README or .env.example) stating that SQLite deployments must stay single-process/single-replica, and that only the PostgreSQL path supports multiple workers -- so a future throughput tweak (adding --workers) doesn't quietly break SQLite installs.", "reviewer_type": "AI_ASSISTED", "status": "CORRECTED", "progress_note": "Fixed. Added the one-line-plus-explanation note right where the finding suggested: directly above DATABASE_URL in .env.example (the file every deployment starts from), stating plainly that app/db/locking.py's advisory-lock coordination is PostgreSQL-only, that a SQLite-backed deployment must stay single-process/single-replica (no uvicorn --workers, no multiple container replicas sharing the file), and pointing to docker-compose.yml's backend/workbench-db services as a working multi-worker-capable PostgreSQL example. Docs-only change; no backend tests affected."}
{"finding_id": "STRUCT-0038", "audit_object": ".github/dependabot.yml", "audit_pass": "tech-debt", "severity": "MATERIAL", "finding": "Dependabot is configured to ignore ALL semver-major updates for both the npm (frontend) and pip (backend) ecosystems (`ignore: [{dependency-name: '*', update-types: ['version-update:semver-major']}]`), on top of a 3-open-PR cap and a monthly cadence. This means a security fix that only ships in a new major version -- exactly the shape of the 'critical Next.js CVE' README's own status table names under issue #20 -- will never surface as an automatic Dependabot PR; catching it depends entirely on someone noticing manually (via CodeQL, a security advisory, or reading changelogs). Reasonable as a deliberate policy to avoid unreviewed breaking bumps landing on `dev`, but the security-relevant blind spot it creates isn't written down anywhere alongside it.", "precise_locations": [".github/dependabot.yml"], "required_correction": "Document the trade-off explicitly (a comment in dependabot.yml or a line in SECURITY.md): major-version bumps are intentionally excluded from automation, so security-relevant major upgrades must be tracked manually -- and consider a narrower carve-out (e.g. Dependabot security updates, which bypass version-type ignores by default in this configuration shape) so at least CVE-driven major bumps still get proposed.", "reviewer_type": "AI_ASSISTED", "status": "CORRECTED", "progress_note": "Fixed, with one factual correction to the finding's own premise worth recording: verified against GitHub's current Dependabot documentation that an `ignore` rule scoped with `update-types` (as this config already does: `update-types: [\"version-update:semver-major\"]`) only affects the routine version-update path -- GitHub's docs state this explicitly: 'update-types only affects version updates, not security updates. Security updates will always be created regardless of the update-types setting.' So this repo's config was never actually silently skipping CVE-driven major-version fixes via that mechanism, contrary to the finding's phrasing -- IF Dependabot security updates (a separate repo setting under Settings > Advanced Security, not controlled by dependabot.yml) is enabled. That caveat is exactly the real gap: nothing in this repo states that assumption or asks anyone to verify it, and this sandbox has no way to check the live repo setting (same GitHub-API/gh-CLI access limitation noted for STRUCT-0032's issue #20 row). Added a file-level comment block to .github/dependabot.yml plus a short comment on each `ignore:` block explaining precisely what the ignore rule does and doesn't suppress, and updated SECURITY.md's 'Dependency vulnerabilities' scope line to say the same and ask whoever administers the GitHub repo to confirm Dependabot security updates is actually turned on. Docs-only change; no backend tests affected; dependabot.yml re-validated with yaml.safe_load()."}
{"finding_id": "STRUCT-0039", "audit_object": "backend/tests/test_destructive_action_authorization.py, backend/tests/test_investigation_authorization.py, backend/tests/test_persisted_identity_roles.py", "audit_pass": "testing-strategy", "severity": "MATERIAL", "finding": "The identical 16-line app.include_router(...) block (one line per route module) is hand-duplicated across 4 separate function-scoped FastAPI app constructions: one each in test_destructive_action_authorization.py and test_investigation_authorization.py, and two in test_persisted_identity_roles.py. No shared fixture or helper exists -- each is a verbatim copy. This is exactly the pattern this session had to work around by hand 8 separate times, once per Stage E split group ('every new/removed router must be proactively updated in all 3 [test files]', confirmed while doing that work): every future router addition or removal requires finding and updating all 4 copies in lockstep, and nothing enforces they stay in sync -- a missed copy silently drops a router from that one test app's coverage without any test failing to flag it.", "precise_locations": ["backend/tests/test_destructive_action_authorization.py:40", "backend/tests/test_investigation_authorization.py:71", "backend/tests/test_persisted_identity_roles.py:92", "backend/tests/test_persisted_identity_roles.py:187"], "required_correction": "Extract a single shared build_test_app() helper (or pytest fixture) in a shared conftest.py that all 3 test files import instead of duplicating the include_router block, so a future router addition/removal touches one place instead of four.", "reviewer_type": "AI_ASSISTED", "status": "CORRECTED", "progress_note": "Fixed, going one step further than the literal required_correction: rather than adding a SECOND hardcoded 16-router list in a new tests/conftest.py (which would just move the duplication problem instead of solving it), extracted a `register_domain_routers(app)` helper into app/main.py itself, right beside create_app(), and had create_app() call it too. This makes main.py -- the actual production wiring -- the single source of truth, so a test app can never silently drift from what production actually registers, which a test-only conftest.py copy could still do. All 5 duplicated copies were replaced (not 4 -- while fixing this I found a 3rd occurrence in test_persisted_identity_roles.py, at test_rotating_revoked_token_reactivates_credential_but_not_disabled_user, that the finding's precise_locations missed): test_destructive_action_authorization.py and test_investigation_authorization.py each had their module-level 16-import + 16-include_router block replaced with `from app.main import register_domain_routers` + `register_domain_routers(app)`; test_persisted_identity_roles.py's 3 inline (function-local) copies were replaced the same way. Verified via the full backend suite (238 tests per --collect-only, all passing unchanged, exit 0) run after syncing to the workbench venv."}
{"finding_id": "STRUCT-0040", "audit_object": "frontend/lib/api-types.ts, frontend/lib/api-validate.ts, frontend/app/page.tsx", "audit_pass": "architecture", "severity": "MATERIAL", "finding": "The frontend hardcodes the connector provider list independently of the backend's CONNECTOR_SPECS single source of truth (STRUCT-0022): api-types.ts's SettingsStatus.connectors and ConnectorCredentialMutation.provider are typed as a literal {aleph, opensanctions} union, api-validate.ts's isSettingsStatus/isConnectorCredentialMutation validators only check those two keys, and page.tsx's settings panel renders a <select> with exactly those two hardcoded <option> values and a credentialProvider state typed 'aleph'|'opensanctions'. Adding the firecrawl connector to the backend registry (this session, app/connectors/registry.py) makes it appear in GET /api/connectors automatically, but the frontend has no way to select it, save a credential for it, or see its status -- the backend and frontend provider lists can now silently diverge the same way the three backend lists did before STRUCT-0022, just one layer further out.", "precise_locations": ["frontend/lib/api-types.ts:78", "frontend/lib/api-types.ts:79", "frontend/lib/api-validate.ts:35", "frontend/lib/api-validate.ts:36", "frontend/app/page.tsx:33", "frontend/app/page.tsx:85"], "required_correction": "Change SettingsStatus.connectors and ConnectorCredentialMutation.provider from a hardcoded literal union to Record<string, ConnectorCredentialStatus> / string, drive the validator from the object's actual keys rather than a fixed pair, and render the settings-panel provider <select> from the GET /api/connectors providers array instead of two hardcoded <option> elements -- so a new backend connector (firecrawl, or any future one) surfaces in the UI without a matching frontend code change.", "reviewer_type": "AI_ASSISTED", "status": "OPEN"}
