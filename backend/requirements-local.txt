# Local development / lint-CI install: layers mypy/ruff/black (and pypdf,
# used by local tooling only) on top of requirements-test.txt, which itself
# layers pytest/pytest-cov on top of requirements.txt. See STRUCT-0026.
-r requirements-test.txt
mypy==1.11.2
ruff==0.6.9
black==26.3.1

pypdf==6.16.1
